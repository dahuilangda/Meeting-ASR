# Development environment variables for the Meeting ASR backend
# Update values as needed for local development.

OPENAI_API_KEY=""
OPENAI_BASE_URL="https://api.openai.com/v1"
OPENAI_MODEL_NAME="gpt-4o-mini"

SECRET_KEY=dev-secret-change-me
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

HF_TOKEN=""
HF_ENDPOINT=https://hf-mirror.com

DATABASE_URL=sqlite:///./sqlite.db

# Allow frontend dev server on localhost:3030 by default
CORS_ORIGINS=http://localhost:3030
DEBUG=true

# Job queue defaults
JOB_QUEUE_MAX_CONCURRENT=3
JOB_QUEUE_MAX_SIZE=50
JOB_QUEUE_MAX_PER_USER=2

# Tooling overrides
FORK_TS_CHECKER_MEMORY_LIMIT=4096
